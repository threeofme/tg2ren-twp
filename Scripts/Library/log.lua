function Init()
 --needed for caching
end

function death(SimAlias, Msg)
	--LogMessage("TWP::DEATH::" .. GetName(SimAlias) .. " " .. Msg)
end

function ctd(SimAlias, Msg)
	--LogMessage("TWP::CTD::" .. GetName(SimAlias) .. " " .. Msg)
end
